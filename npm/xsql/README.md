# xsql-cli

AI-first cross-database CLI tool with SSH proxy support.

## Installation

```bash
npm install -g xsql-cli
```

## Usage

```bash
xsql query "SELECT 1" -p dev -f json
xsql version
```

For full documentation, see [GitHub](https://github.com/zx06/xsql).
